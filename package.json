{
  "name": "tanzania-real-estate-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "preview": "cd frontend && npm run preview",
    "cf-build": "cd frontend && npm ci && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
